import { FilterTripsPipe } from './filter-trips.pipe';

describe('FilterTripsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTripsPipe();
    expect(pipe).toBeTruthy();
  });
});
